name: Build and Publish Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.G_TOKEN }}

      - name: Build Docker image
        run: |
          docker buildx build --platform linux/arm64 -t ghcr.io/kuizw9/vmqfox-backend:arm64 .
          docker buildx build --platform linux/amd64 -t ghcr.io/kuizw9/vmqfox-backend:amd64 .
      - name: Push Docker image
        run: |
          docsker push ghcr.io/kuizw9/vmqfox-backend:amd64
          docsker push ghcr.io/kuizw9/vmqfox-backend:arm64